ANTHROPIC_API_KEY=your-api-key-here

# Optional: Jina Reader API key for fetching job postings from URLs.
# Without this, the fetch_job_posting tool still works but uses the
# unauthenticated free tier (~20 requests/min). Get a free key at jina.ai.
# JINA_API_KEY=your-jina-key-here
